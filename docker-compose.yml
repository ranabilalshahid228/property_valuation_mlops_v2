version: '3.9'
services:
  property-valuation:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./models:/app/models
      - ./logs:/app/logs
      - ./reports:/app/reports
      - ./data:/app/data
    environment:
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
